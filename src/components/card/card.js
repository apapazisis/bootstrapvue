import CardBody, {props as bodyProps} from './card-body';

export default {
    name: 'card',
    props: bodyProps,
    render(h) {

        let components = [];

        return h(CardBody, {

        })
    }
}